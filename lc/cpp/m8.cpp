#include <iostream>

class Solution {
public:
    bool isDigit(char a) {
        if(a >= '0' && a <= '9') return true;
        else return false;
    }
    int myAtoi(std::string s) {
        for(int i = 0;i<s.size();i++) {
            if (s[i] >= '0' && s[i] <='9') {
                if()
            }
        }
        return a;
    }
};

int main() {
    Solution a;
    std::cout << a.myAtoi("hello");
}